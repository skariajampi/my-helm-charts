apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.kafka.serviceName }}
  labels:
    app: {{ .Values.kafka.name }}
spec:
  clusterIP: None
  selector:
    app: {{ .Values.kafka.name }}
  ports:
  - port: 9092
    name: broker
